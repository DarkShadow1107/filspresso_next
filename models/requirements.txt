# Optional legacy chemistry/image dependencies.
# These are no longer installed by Dockerfile.ai in the llama.cpp runtime path,
# but are kept for optional local experiments.
opencv-python-headless>=4.5.5
albumentations==1.1.0
timm>=0.9.0
einops>=0.6.0
configargparse
pyonmttok>=1.23,<2
rdkit-pypi>=2021.03.2
