# Filspresso workspace Python requirements (local scripts + optional tooling)
# NOTE: Dockerfile.ai installs requirements.ai.txt for the runtime gateway image.

# Web framework
Flask==3.1.3
Flask-CORS==6.0.0
waitress==3.0.1

# Tanka runtime (llama.cpp gateway + lexical retrieval)
Pillow>=10.0.0

# Database
psycopg2-binary
SQLAlchemy==2.0.19

# Utilities
numpy>=1.25,<2.0
pandas>=2.0.0
python-dotenv==1.0.0
requests==2.33.0

# Chemistry data sources
chembl-webresource-client>=0.10.9
rdkit>=2023.9.1
py3Dmol>=2.0.4

# RAG PDF parsing (legacy data ingestion)
PyPDF2>=3.0.0
